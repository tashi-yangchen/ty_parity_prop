Gem::Specification.new do |spec|
    spec.authors = ["Champion Dolphin"]
    spec.files = Dir["lib/**/*.rb"]
    spec.name = "ty_parity_prop"
    spec.summary = "Tashi's gem to partition an array of integer based on parity"
    spec.version = "0.0.1"
end